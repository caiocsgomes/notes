# How to update initial message on linux terminal

The initial message in linux is known as `motd` (message of the day) and it is set on `/etc/motd` file, so to set it just place the message on the file.